services:
  - type: web
    name: marketradar-api
    env: python
    plan: free
    buildCommand: pip install --upgrade pip
    startCommand: python server.py
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
